from setuptools import setup, find_packages

setup(
    name="AnkiTemplateDesigner",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    include_package_data=True,
    description="Anki add-on for template design with UI testing support.",
    author="Your Name",
    author_email="your.email@example.com",
)